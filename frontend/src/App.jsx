import AppRouter from './routes/AppRouter.jsx';

/**
 * Application entry point.
 * Simply renders the AppRouter, which manages layout and routing.
 */
export default function App() {
  return <AppRouter />;
}
