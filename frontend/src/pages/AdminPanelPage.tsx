/**
 * TicketS - AdminPanelPage (placeholder)
 *
 * Panel de administración global de la plataforma.
 * Protegida: requiere autenticación (futuro: role super_admin).
 * Pendiente de implementar con el diseño del proyecto.
 */
export default function AdminPanelPage() {
  return (
    <main className="mx-auto max-w-6xl px-4 py-20 text-center">
      <h1 className="text-3xl font-black tracking-tight text-white">
        Panel de administración
      </h1>
      <p className="mt-3 text-red-100/60">
        Gestiona usuarios, organizaciones y configuración global de la plataforma.
      </p>
      <p className="mt-8 text-sm text-red-100/40">
        Esta sección estará disponible próximamente.
      </p>
    </main>
  );
}
